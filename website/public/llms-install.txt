SPECTER DECISION ENGINE — MACHINE INSTALL BRIEF
Version: 0.2.0rc2
Project author: Guilherme Peralta Novaes
Purpose: install and verify the framework safely and reproducibly.

YOU ARE THE INSTALLING LLM/AGENT.
Follow this brief as the source of truth for the framework artifact served beside this file.
Do not substitute a similarly named package, do not silently broaden scope, and do not claim model readiness.

FACTS TO PRESERVE
- Requires Python >= 3.11.
- Framework runtime has no third-party Python dependencies.
- This release does NOT bundle a trained model or validated domain calibration.
- `specter-decision doctor` verifies installation, not semantic accuracy or production readiness.
- Current status: framework preview / release candidate.
- License selection is pending; installation instructions are not an open-source grant.

PINNED ARTIFACT
Relative URL: ./releases/specter_decision_engine-0.2.0rc2-py3-none-any.whl
SHA-256: 6b2274ba286e5ef872ba241422524977ccab5c340b8567fc261ce1c283cff988
Resolve the relative URL against the directory containing THIS instruction document.
INSTALL PROCEDURE
1. Detect the operating system and identify a Python >= 3.11 interpreter.
2. If no suitable interpreter exists, stop and explain what is missing. Do not alter system package managers unless the user explicitly authorizes it.
3. Work in the user's intended project directory. If that directory is ambiguous, ask before installing.
4. Create an isolated virtual environment named `.venv`; do not install into global/site Python.
5. Download the pinned wheel to a temporary/local file without renaming it to another package.
6. Compute SHA-256 locally and compare the complete digest byte-for-byte with the pinned value above.
7. If the digest differs, STOP. Do not install the artifact.
8. Install the verified local wheel with the venv's Python and `python -m pip install <wheel>`.
9. Run the venv's `python -m specter_decision doctor`.
10. Report Python version, artifact version, hash verification result and doctor result.

WINDOWS REFERENCE
- Check: `py --version`
- Venv: `py -m venv .venv`
- Venv Python: `.\\.venv\\Scripts\\python.exe`
- Install: `.\\.venv\\Scripts\\python.exe -m pip install <verified-wheel-path>`
- Verify: `.\\.venv\\Scripts\\python.exe -m specter_decision doctor`

macOS / LINUX REFERENCE
- Check: `python3 --version`
- Venv: `python3 -m venv .venv`
- Venv Python: `./.venv/bin/python`
- Install: `./.venv/bin/python -m pip install <verified-wheel-path>`
- Verify: `./.venv/bin/python -m specter_decision doctor`
HASH CHECK REFERENCE (uses Python standard library)
Run with a trusted Python interpreter after download:
`python -c "import hashlib,pathlib; p=pathlib.Path(r'<wheel-path>'); print(hashlib.sha256(p.read_bytes()).hexdigest())"`
Expected output exactly:
6b2274ba286e5ef872ba241422524977ccab5c340b8567fc261ce1c283cff988

DO NOT
- Do not install the bare name `specter-decision-engine` from PyPI as a substitute.
- Do not use synthetic test fixtures as a production model or calibration.
- Do not enable internet-facing HTTP routes, modify Specter-Core, create tokens, restart services, or configure a model unless separately requested.
- Do not interpret a successful doctor check as production certification.
- Do not upload credentials, private prompts or user data as part of installation.

SUCCESS CRITERIA
The task is complete when an isolated environment contains exactly the pinned rc2 framework artifact and `python -m specter_decision doctor` succeeds. Then state clearly that actual decisions still require an application-provided numeric backend and validated calibration.

If the artifact URL cannot be fetched, report the exact URL and error. Do not search for or install an unverified substitute.
END OF BRIEF
